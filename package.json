{
  "name": "search_with_ai",
  "description": "ai search chat with node.js",
  "version": "1.2.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "packageManager": "yarn@3.5.1",
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
